!function(){"use strict";var e=document.getElementById("burger"),t=document.getElementById("nav");e&&t&&(e.addEventListener("click",function(){var n=t.classList.toggle("ouvert");e.setAttribute("aria-expanded",n?"true":"false"),e.setAttribute("aria-label",n?"Fermer le menu":"Ouvrir le menu")}),t.querySelectorAll("a").forEach(function(n){n.addEventListener("click",function(){t.classList.remove("ouvert"),e.setAttribute("aria-expanded","false")})})),document.querySelectorAll(".faq-question").forEach(function(e){e.addEventListener("click",function(){var t=e.parentElement.nextElementSibling||e.closest(".faq-item").querySelector(".faq-reponse"),n="true"===e.getAttribute("aria-expanded");e.setAttribute("aria-expanded",n?"false":"true"),t&&(t.hidden=n)})});var n=document.getElementById("champ-formule"),i=document.getElementById("formule-choisie"),o=document.getElementById("formule-choisie-nom"),a=document.getElementById("formule-retirer");function r(e){n&&(n.value=e),i&&o&&(o.textContent=e,i.hidden=!1)}if(document.querySelectorAll("[data-formule]").forEach(function(e){e.addEventListener("click",function(t){var n=e.getAttribute("data-formule"),i=document.getElementById("contact");i?(t.preventDefault(),r(n),i.scrollIntoView({behavior:"smooth"})):(t.preventDefault(),window.location.href="/?formule="+encodeURIComponent(n)+"#contact")})}),n&&window.location.search){var c=new URLSearchParams(window.location.search).get("formule");c&&r(c.slice(0,80))}if(window.location.hash.length>1){var s=document.getElementById(window.location.hash.slice(1));s&&window.addEventListener("load",function(){s.scrollIntoView({behavior:"instant",block:"start"})})}a&&a.addEventListener("click",function(){n&&(n.value="Non précisée"),i&&(i.hidden=!0)});var l=document.getElementById("formulaire"),d=document.getElementById("form-erreur"),u=document.getElementById("bouton-envoyer");function m(e){d&&(d.textContent=e,d.hidden=!1,d.scrollIntoView({behavior:"smooth",block:"center"}))}l&&l.addEventListener("submit",function(e){e.preventDefault(),d&&(d.hidden=!0,d.textContent="");var t=document.getElementById("nom"),n=document.getElementById("activite"),i=document.getElementById("telephone"),o=document.getElementById("email"),a=document.getElementById("message");if([t,n,i,o,a].forEach(function(e){e&&e.classList.remove("champ-invalide")}),!t.value.trim())return t.classList.add("champ-invalide"),m("Indiquez votre prénom et votre nom."),void t.focus();if(!n.value.trim())return n.classList.add("champ-invalide"),m("Indiquez votre activité, pour que je sache à qui je parle."),void n.focus();var r=""!==i.value.trim(),c=""!==o.value.trim();if(!r&&!c)return i.classList.add("champ-invalide"),o.classList.add("champ-invalide"),m("Laissez-moi au moins un téléphone ou un email, sinon je ne peux pas vous répondre."),void i.focus();if(c&&!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(o.value.trim()))return o.classList.add("champ-invalide"),m("Cet email ne semble pas valide. Vérifiez-le, ou laissez plutôt votre téléphone."),void o.focus();if(r&&!/^[0-9+\s().-]{6,20}$/.test(i.value.trim()))return i.classList.add("champ-invalide"),m("Ce numéro de téléphone ne semble pas valide. Vérifiez-le."),void i.focus();if(!a.value.trim())return a.classList.add("champ-invalide"),m("Dites-moi quelques mots sur votre activité."),void a.focus();u.disabled=!0,u.textContent="Envoi en cours…";var s=new FormData(l);fetch("https://api.web3forms.com/submit",{method:"POST",body:s,headers:{Accept:"application/json"}}).then(function(e){return e.json()}).then(function(e){if(!e.success)throw new Error(e.message||"Échec de l'envoi");document.body.classList.add("transition-sortie");window.setTimeout(function(){window.location.href="merci.html"},320)}).catch(function(){m("L'envoi n'a pas fonctionné. Réessayez dans un instant, ou écrivez-moi directement par email (adresse en bas de page)."),u.disabled=!1,u.textContent="Envoyer ma demande"})});var v=document.querySelectorAll("section:not(.hero) > .conteneur > *, .carte, .etapes-liste li, .faq-item");if("IntersectionObserver"in window){var f=new IntersectionObserver(function(e){e.forEach(function(e){e.isIntersecting&&(e.target.classList.add("visible"),f.unobserve(e.target))})},{threshold:.08});var ojP=[],ojR=[];v.forEach(function(e){if(e.getBoundingClientRect().top<window.innerHeight)return;var t=e.parentElement,i=ojP.indexOf(t);-1===i&&(ojP.push(t),ojR.push(0),i=ojP.length-1);var d=80*ojR[i]++;d>400&&(d=400),d>0&&(e.style.transitionDelay=d+"ms"),e.classList.add("apparait"),f.observe(e)})}!function(){var e=document.querySelector(".hero");if(e){var t=e.querySelector(".maquette-imac"),n=e.querySelector(".hero-texte");if(t){var i=window.matchMedia("(prefers-reduced-motion: reduce)").matches,o=window.matchMedia("(hover: none)").matches;if(!(i||o||window.innerWidth<900)){var a=null,r=0,c=0;e.addEventListener("mousemove",function(t){var n=e.getBoundingClientRect();r=(t.clientX-n.left)/n.width*2-1,c=(t.clientY-n.top)/n.height*2-1,r<-1?r=-1:r>1&&(r=1),c<-1?c=-1:c>1&&(c=1),null===a&&(a=window.requestAnimationFrame(s))}),e.addEventListener("mouseleave",function(){null!==a&&(window.cancelAnimationFrame(a),a=null),t.style.transform="",n&&(n.style.transform="")})}}}function s(){a=null,t.style.transform="perspective(1300px) rotateY("+8*r+"deg) rotateX("+5*-c+"deg) translateX("+9*r+"px)",n&&(n.style.transform="translateX("+-5*r+"px)")}}()}();
/* ===== Barre de progression de lecture ===== */
(function () {
  "use strict";

  var barre = document.createElement("div");
  barre.className = "progression-lecture";
  document.body.appendChild(barre);

  var frame = null;

  function maj() {
    frame = null;
    var hauteur = document.documentElement.scrollHeight - window.innerHeight;
    var pourcentage = hauteur > 0 ? (window.scrollY / hauteur) * 100 : 0;
    barre.style.width = pourcentage + "%";
  }

  window.addEventListener("scroll", function () {
    if (frame === null) frame = window.requestAnimationFrame(maj);
  }, { passive: true });

  window.addEventListener("resize", maj);
  maj();
})();

/* ===== Parallaxe légère au scroll =====
   S'applique aux éléments qui portent [data-parallax="0.xx"]. Le facteur fixe
   l'amplitude du décalage. On lit aussi data-parallax-base pour ne pas écraser
   une éventuelle rotation ou tout autre transform déjà posé en CSS.
   Le décalage est plafonné (data-parallax-max, en pixels) : sans ça, plus on
   scrolle loin du centre de l'écran, plus l'élément dérive — sur mobile, la
   photo de « Mon histoire » finissait par glisser sur le texte juste en
   dessous. Avec un plafond, le mouvement reste toujours visible mais ne
   dépasse jamais une poignée de pixels, quelle que soit la distance de scroll. */
(function () {
  "use strict";

  var elements = document.querySelectorAll("[data-parallax]");
  if (!elements.length) return;
  if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) return;

  var frame = null;

  function maj() {
    frame = null;
    var milieu = window.innerHeight / 2;

    elements.forEach(function (element) {
      var rect = element.getBoundingClientRect();
      var distance = rect.top + rect.height / 2 - milieu;
      var facteur = parseFloat(element.getAttribute("data-parallax")) || 0.1;
      var max = parseFloat(element.getAttribute("data-parallax-max")) || 14;
      var base = element.getAttribute("data-parallax-base") || "";
      var decalage = distance * facteur * -1;
      if (decalage > max) decalage = max;
      else if (decalage < -max) decalage = -max;
      element.style.transform = base + " translateY(" + decalage.toFixed(1) + "px)";
    });
  }

  window.addEventListener("scroll", function () {
    if (frame === null) frame = window.requestAnimationFrame(maj);
  }, { passive: true });

  maj();
})();

/* ===== Transitions de page douces =====
   On n'intercepte jamais le clic (les ouvertures en nouvel onglet, les clics
   du milieu, etc. continuent de fonctionner normalement) : on ajoute juste
   une classe qui fait doucement disparaître la page pendant que le navigateur
   charge la suivante, pour éviter le blanc brutal. */
(function () {
  "use strict";

  if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) return;

  function estUneNavigationInterne(lien) {
    if (!lien || lien.origin !== window.location.origin) return false;
    if (lien.target === "_blank" || lien.hasAttribute("download")) return false;
    var href = lien.getAttribute("href") || "";
    if (/^(mailto:|tel:|javascript:|#)/.test(href)) return false;
    return true;
  }

  document.addEventListener("click", function (evenement) {
    if (evenement.defaultPrevented || evenement.button !== 0) return;
    if (evenement.metaKey || evenement.ctrlKey || evenement.shiftKey || evenement.altKey) return;

    var lien = evenement.target.closest("a[href]");
    if (!lien || !estUneNavigationInterne(lien)) return;

    var memePage = lien.pathname === window.location.pathname;
    if (memePage && (lien.hash || lien.search === window.location.search)) return;

    document.body.classList.add("transition-sortie");
  });

  window.addEventListener("pageshow", function (evenement) {
    if (evenement.persisted) document.body.classList.remove("transition-sortie");
  });
})();

/* ===== Comparateur avant / après ===== */
(function () {
  "use strict";

  var curseur = document.getElementById("aa-curseur");
  var avant = document.getElementById("aa-avant");
  var diviseur = document.getElementById("aa-diviseur");
  var cadre = document.querySelector(".aa-cadre");
  if (!curseur || !avant || !diviseur || !cadre) return;

  function maj() {
    var v = curseur.value;
    avant.style.clipPath = "inset(0 " + (100 - v) + "% 0 0)";
    diviseur.style.left = v + "%";
  }

  curseur.addEventListener("input", maj);
  maj();

  /* Petite démo automatique : la première fois que le comparateur entre
     dans l'écran, le curseur balaie une fois de gauche à droite pour
     montrer qu'il est manipulable, puis se pose au centre et rend
     entièrement la main au visiteur. Coupée si l'utilisateur touche déjà
     le curseur (souris, tactile ou clavier) ou préfère moins de mouvement. */
  if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) return;
  if (!("IntersectionObserver" in window)) return;

  var idAnimation = null;
  var demoDesactivee = false;
  var observateur = null;

  function definirValeur(v) {
    curseur.value = v;
    maj();
  }

  function assouplir(t) {
    return t < 0.5 ? 2 * t * t : 1 - Math.pow(-2 * t + 2, 2) / 2;
  }

  /* Désactive la démo pour de bon, quel que soit le moment où l'utilisateur
     touche le curseur : avant, pendant ou juste après le déclenchement de
     l'observateur. Sans ce verrou définitif, une interaction qui coïncide
     avec le déclenchement de l'observateur pouvait être écrasée par la
     démo qui démarre ou continue juste après. */
  function arreterDemo() {
    demoDesactivee = true;
    if (idAnimation !== null) {
      cancelAnimationFrame(idAnimation);
      idAnimation = null;
    }
    if (observateur) observateur.disconnect();
  }

  function jouerDemo() {
    if (demoDesactivee || document.activeElement === curseur) return;

    var depart = null;
    var duree = 2200;
    definirValeur(0);

    function etape(horodatage) {
      if (demoDesactivee) return;
      if (depart === null) depart = horodatage;
      var t = Math.min((horodatage - depart) / duree, 1);
      var valeur;
      if (t < 0.75) {
        valeur = assouplir(t / 0.75) * 100;
      } else {
        valeur = 100 - assouplir((t - 0.75) / 0.25) * 50;
      }
      definirValeur(valeur);
      if (t < 1) {
        idAnimation = requestAnimationFrame(etape);
      } else {
        definirValeur(50);
        idAnimation = null;
      }
    }

    idAnimation = requestAnimationFrame(etape);
  }

  ["pointerdown", "touchstart", "keydown", "focus"].forEach(function (evenement) {
    curseur.addEventListener(evenement, arreterDemo, { passive: true });
  });

  observateur = new IntersectionObserver(
    function (entrees, obs) {
      entrees.forEach(function (entree) {
        if (entree.isIntersecting) {
          jouerDemo();
          obs.disconnect();
        }
      });
    },
    { threshold: 0.4 }
  );
  observateur.observe(cadre);
})();

/* ===== Formulaire : petites coches de validation en temps réel =====
   Vient en complément de la validation au clic sur « Envoyer » (plus haut) :
   ici, on rassure ou on alerte dès que la personne quitte un champ. */
(function () {
  "use strict";

  var nom = document.getElementById("nom");
  var activite = document.getElementById("activite");
  var telephone = document.getElementById("telephone");
  var email = document.getElementById("email");
  var message = document.getElementById("message");
  if (!nom) return;

  function icone(champ) {
    return champ.parentElement.querySelector(".champ-icone");
  }

  function marquerValide(champ) {
    champ.classList.remove("champ-invalide");
    champ.classList.add("champ-valide");
    var ic = icone(champ);
    if (ic) ic.classList.add("visible");
  }

  function marquerNeutre(champ) {
    champ.classList.remove("champ-invalide", "champ-valide");
    var ic = icone(champ);
    if (ic) ic.classList.remove("visible");
  }

  function marquerInvalide(champ) {
    champ.classList.remove("champ-valide");
    var ic = icone(champ);
    if (ic) ic.classList.remove("visible");
    champ.classList.remove("champ-invalide");
    void champ.offsetWidth; // relance l'animation de secousse à chaque fois
    champ.classList.add("champ-invalide");
  }

  function valideRempli(champ) {
    if (!champ.value.trim()) marquerInvalide(champ);
    else marquerValide(champ);
  }

  function valideEmail() {
    var v = email.value.trim();
    if (!v) { marquerNeutre(email); return; }
    if (/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(v)) marquerValide(email);
    else marquerInvalide(email);
  }

  function valideTelephone() {
    var v = telephone.value.trim();
    if (!v) { marquerNeutre(telephone); return; }
    if (/^[0-9+\s().-]{6,20}$/.test(v)) marquerValide(telephone);
    else marquerInvalide(telephone);
  }

  if (nom) nom.addEventListener("blur", function () { valideRempli(nom); });
  if (activite) activite.addEventListener("blur", function () { valideRempli(activite); });
  if (message) message.addEventListener("blur", function () { valideRempli(message); });
  if (email) email.addEventListener("blur", valideEmail);
  if (telephone) telephone.addEventListener("blur", valideTelephone);
})();
